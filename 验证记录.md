# 珊瑚小屋版 · 素材替换与流程验收

验证日期：2026-09-09。运行环境：本机 Google Chrome / Playwright，桌面 1280×960、手机视口 390×844。测试使用独立浏览器存储，不覆盖玩家存档。

## 替换结果

- 唯一画风基准：珊瑚橙弧形屋顶小屋；头顶小黄鸟的卡皮巴拉；闭眼捧叶熊猫。
- 网页只读取 `assets/coral/`：背景 1 张、动物 2 张、植物 20 张、装饰 6 张、图标 16 张、地块状态 3 张，共 48 张。
- 单体已拆分、去除外部白底/棋盘底，并统一画布与落地锚点。47 张单体 PNG 含真实 Alpha；背景保持不透明。视觉检查见素材总览。
- 土地改为 4 列 × 2 排的右倾斜平行四边形；指针命中按土地区域四边形判定，透明角落不会触发该地块。
- 奶油色面板、暖棕描边、浅绿按钮、商店/仓库/播种/收获/编辑/设置图标已统一更新。
- 保留原手绘版备份。旧存档保留金币、经验、库存、已种植物和开垦状态；装饰若与新土地重叠则迁移到空地，无空位时回仓库。

## 验证通过

| 测试 | 结果 |
| --- | --- |
| `node --test tests/model.test.cjs` | 10/10，通过：经济闭环、成长阶段、动物技能、购买限制、开垦顺序、摆放、存档、斜向命中、旧版迁移 |
| `tests/browser.cjs` | `BROWSER_CORE_FLOW_OK`：真实时间新手引导、拖过四格播种、等待与收获、出售、升二级、买动物/装饰、开垦、摆放/取消、刷新及离线成长 |
| `tests/advanced.cjs` | `ADVANCED_OK`：五种作物连续收获且不重复计数、出售数量上限、熊猫价格加成、取消出售、动物收回/恢复、编辑回滚、触屏双指缩放/拖动边界、重置取消与确认 |
| `tests/progression.cjs` | `BROWSER_LEVEL_1_TO_5_OK`：全新存档通过实际界面购买/种植/出售升至五级，开垦八格、买齐两只动物和五种装饰、摆放并刷新保存；仅加速经过的时间，没有注入经验、金币或库存 |
| 单文件离线 | 禁用网络后打开 `习惯花园-双击即玩.html`，素材全部加载、免费首种成功、刷新后种植存档保留 |
| 页面资源 | 流程测试未出现 JS/Console 错误；升级测试检查 HTTP 错误及资源请求，全部使用新素材路径 |
| 布局 | 检查桌面、手机、商店与五级花园截图；手机无横向页面溢出 |

升级轨迹（累计经验 / 剩余金币）：Lv.2 25/100 → Lv.3 57/124 → Lv.4 105/160 → Lv.4 169/214 → Lv.5 233/268。之后继续种植南瓜，完成全部购买与开垦。

## 留存截图

- `tests/initial-desktop.png`：初始花园。
- `tests/level2-desktop.png`、`tests/level2-mobile.png`：实际新手经营后的花园。
- `tests/level5-mobile.png`：高级功能测试布置，展示五种成熟植物、动物与装饰（该张使用五级测试存档）。
- `tests/progression-level5-mobile.png`：实际从一级经营到五级后的存档。
- `tests/shop-mobile.png`：手机商店。
- `tests/standalone-offline-mobile.png`：断网打开单文件并播种。

单文件重新构建为约 8.3 MB。浏览器测试脚本中的 Chrome/Playwright 路径与此机器一致；换机器运行测试需要调整这两个路径。验证的是网页端 Chrome 与手机视口/触摸模拟，未声称原生 App 或手机真机验收。

## 悬浮操作布局更新

去除顶部与底部整条面板，地图铺满游戏容器；等级、金币、仓库及底部五个入口固定悬浮，使用独立奶油色小底。提示与展开的工具也改成悬浮布局。地图点击与经营数值不变。

重新通过 `BROWSER_CORE_FLOW_OK` 和 `ADVANCED_OK`，覆盖新手、播种、收获、出售、动物、编辑、存档、触屏地图与离线单文件。另检查 320×640、390×600、390×844：展开播种工具后八块土地中心均未被操作控件遮挡。旧面板布局代码留存于 `历史版本/面板版/`。

## 手机与 iPad 适配

`tests/responsive.cjs` 通过桌面 1280×960（游戏区域精确 375×812）、手机 375×812 / 320×640 / 390×844、iPad 768×1024 / 820×1180 / 1024×1366 / 1180×820 共八组浏览器视口。检查无页面横向溢出，展开工具后八块土地中心均可点击，实际播种及商店购买成功，无页面脚本错误。修复平板横屏工具遮挡：展开工具时将地图上移至土地可操作范围。截图为 tests/responsive-*.png。此为浏览器尺寸与触摸模拟验证。

## 土地播种气泡

底部种子栏改为土地上方固定 282px 气泡，显示约 3.5 个植物及库存，支持横向滑动。点击植物种当前土地；向外拖出收起气泡，可连续划过空土地，松手自动结束。拖动跳过占用/未开垦地块，同次经过不重复扣库存。库存为零或未解锁的植物不可播种。

`tests/seed-bubble.cjs` 通过首株免费→收获→出售、点击选定地块、连续拖种、跳过占用格、重复经过、横滑不误种、滚动后选择南瓜与库存扣减。旧测试的播种入口已同步为气泡。


## 2026-09-10 收获气泡与底栏调整

- 成熟土地打开镰刀气泡；单击收获选中土地，拖动连续收获经过的成熟土地。点击完成或开始拖动即关闭气泡，无底部收获工具和完成按钮。
- 底栏仅保留商店、编辑、设置；奶油色整体圆角面板、竖分隔线；底栏及相邻操作区整体上移 20px。
- 已通过 JavaScript 语法检查、10 项模型测试、3 项收获手势测试（单击目标、连续拖动及跳过未成熟/重复土地、取消操作）。
- 已重新生成 dist 与离线双击版。本轮未进行浏览器视觉或真机验收，未发布线上站点。


## 2026-09-10 顶部 HUD 与双指缩放
- 去掉缩放和定位按钮，商店、编辑、设置移到仓库下方竖排，无整体底板。
- 顶部改为幼苗徽章、Lv.等级、横向经验条、金币胶囊、横向仓库按钮。
- 双指以手势中心缩放与平移，上限 1.8 倍；松手不触发土地点击。
- 通过语法检查、模型与手势单元测试；已更新静态构建和离线双击版。未做浏览器视觉及手机真机验收，未更新线上站点。


## 2026-09-11 后半段交互补验收

补验 9 月 10 日晚上之后落地、验证记录尚未写到的功能：动物技能气泡、长按进入编辑与收回、新手引导手指统一、土地间距、压缩素材。运行环境仍为本机 Google Chrome / Playwright；桌面 1280×960、手机视口 390×844。测试使用独立浏览器存储，不覆盖玩家存档。未做手机真机验收，未更新线上站点。

### 覆盖范围

- 点击出场动物弹出技能气泡：卡皮巴拉「成熟时间减少」、熊猫「出售价格更高」；再点同一只约 0.8 秒后关闭；点另一只时只保留当前气泡。
- 引导完成后，长按已摆放装饰进入编辑并打开装饰分页；长按出场动物进入编辑并打开动物分页。选中后出现「收回」，点收回后物品回编辑栏；点「取消」恢复编辑前布置。
- 新手引导手指统一为 76px 基准图 `icon_tutorial_hand.png`：第一块土地、右上角仓库、仓库「确认出售」都会出现指向手指。
- 商店 / 编辑 / 设置在仓库下方右侧竖排，三项入口均在。
- 花园背景与过大图标保持压缩体积：背景 < 700KB，镰刀与引导手指 < 20KB，幼苗徽章 < 90KB。

### 验证通过

| 测试 | 结果 |
| --- | --- |
| `node --test tests/model.test.cjs tests/harvest-gesture.test.cjs tests/edit-grid.test.cjs tests/late-features.test.cjs` | 19/19。修复后半段引入的两处过期断言：手势沙箱补上 `storeBubble`；旧存档迁移目标版本改为当前 `layoutVersion=4` |
| `tests/late-features.cjs` | `LATE_FEATURES_OK`：真实时间走完种植→收获→仓库手指→出售手指；注入五级存档后点动物说话、再点关闭、换一只说话；长按装饰收回并取消回滚；长按动物进入编辑动物页；无页面脚本错误；素材全部加载 |
| `tests/seed-bubble.cjs` | `SEED_BUBBLE_OK`：作为回归，确认播种气泡、连续拖种和出售引导未被后半段改坏 |
| `tests/browser.cjs` | `BROWSER_CORE_FLOW_OK`：已改为镰刀气泡收获和「收回」；覆盖新手、四格拖种、出售、升二级、买动物/装饰、开垦、摆放、刷新和离线成熟 |
| `tests/advanced.cjs` | `ADVANCED_OK`：五种作物连续拖收、出售数量上限、熊猫加价、取消出售、长按收回动物并取消回滚、双指缩放上限 1.8、重置、断网打开单文件 |
| `tests/progression.cjs` | `BROWSER_LEVEL_1_TO_5_OK`：不注入金币经验，从一级经营到五级。轨迹 Lv.2 25/100 → Lv.3 57/124 → Lv.4 105/160 → Lv.4 169/214 → Lv.5 233/268 |
| `tests/responsive.cjs` | 八组视口仍通过，脚本本身不需改入口 |

旧脚本已去掉 `#harvestMode`、`exit-mode`、`edit-store`、`edit-add` 和失效摆放坐标，改为当前镰刀气泡、「收回」和合法草地格子。未做手机真机验收，未更新线上站点。

### 留存截图

- `tests/late-tutorial-land.png`：第一块土地上的引导手指，右侧竖排商店 / 编辑 / 设置。
- `tests/late-tutorial-warehouse.png`：收获后仓库按钮上的引导手指。
- `tests/late-tutorial-sell.png`：仓库「确认出售」旁的引导手指。
- `tests/late-animal-talk.png`：卡皮巴拉技能气泡。
- `tests/late-edit-store.png`：长按装饰进入编辑后，花盆上方「收回」与底部取消 / 保存。
