# 昼夜与天气第一版

- 每轮 300 秒：白天 150 秒、黄昏 45 秒、夜晚 75 秒、天亮 30 秒。黄昏、天亮使用连续平滑过渡。
- 页面在前台时推进；切换后台暂停，重新打开或刷新从白天开始。植物离线成长与存档保持原有规则。
- 首次降雨判定在 60–90 秒后；每次有 45% 概率降雨，未命中则 30 秒后再判定。雨持续 40–70 秒（包括淡入淡出），结束后至少间隔 75–105 秒再判定。不保证每轮下雨。
- 小屋亮窗与门灯使用独立 SVG 覆盖层，原背景未修改；已摆放庭院灯随地图位置叠加光晕。
- 雨层不拦截点击；界面不受场景调暗影响。降低动态效果偏好开启时雨丝静止显示。
- 已同步 dist 静态文件和「习惯花园-双击即玩.html」。没有发布到线上。

验证：13 项测试通过（原有模型 10 项、昼夜天气 3 项）；JavaScript 语法检查、静态引用与离线包检查通过，本地 HTTP 200。尚未进行浏览器视觉和交互验收。

## 第二版视觉优化

黄昏前 15 秒升至金橙色并保持到 195 秒，夜晚前 15 秒转为蓝紫色。全周期仍为 300 秒。使用 multiply 和 soft-light 混色层保留纹理，加强暖窗和地面光晕，增加远近两层斜雨丝。天气标签移至经验条下方，跟随 HUD 尺寸定位。

验证：13 项逻辑测试通过，包含冷暖颜色在阶段边界的连续性与黄昏稳定段；语法、静态资源引用和离线包检查通过。尚未进行浏览器视觉验收。

## 第三版物件可读性与萤火虫

背景图、完整环境混色独立到 scenery 层。土地、植物、动物、装饰改用轻量调色：黄昏保持亮度，夜晚亮度系数为 0.86，不再受背景深蓝混色覆盖。夜晚画面顶部与底部各 5 只随机初始位置、独立缓慢漂移的萤火虫，雨天减弱，白天消失；降低动态效果偏好下停止飞舞与明暗脉动，切后台沿用暂停机制。天气标签仍与经验栏左边缘对齐。

验证：13 项模型与环境逻辑测试通过；390×844 独立浏览器验证白天、黄昏、夜晚截图、标签对齐、商店打开与成熟地块点击通过，Console 无错误。测试使用独立浏览器临时状态，不改用户存档。截图见 tests/environment-day.png、environment-dusk.png、environment-night.png。本地 dist 和双击即玩版本已同步，未发布线上。

## 第四版协调调色与双建筑灯光

降低背景黄昏混色的黄橙强度；前景从独立亮度滤镜改为 RGB 色彩矩阵，黄昏稍暖、夜晚适度降低红绿通道，维持可辨认度并贴近环境。夜景背景蓝色也略柔化。右侧小店新增屋檐灯芯、柜台泛光和门前光斑，左侧增加门前墙脚反光；沿用地图定位，无需拆背景。

验证：390×844 白天、黄昏、夜晚截图已查看；经验栏与天气标签对齐、商店打开、成熟地块点击通过；浏览器控制台无错误。静态和离线输出已同步，线上未更新。

## 第五版月光与草边

新增右上方柔和月光、斜向漫射光与下方池塘冷色反光，仅夜晚显示，雨天弱化。土地原图中的绿色草边通过夜间单独降低土层饱和度至 0.65、亮度至 0.92 弱化，植物不受该土层滤镜影响。下方萤火虫由 5 只增至 10 只，上方保留 5 只。

验证：独立浏览器检查昼夜渲染、标签对齐、商店与成熟土地点击通过，Console 无错误。静态目录与离线单文件已同步，未部署线上。
