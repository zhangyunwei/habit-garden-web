# 习惯花园 · 珊瑚小屋网页可玩版

## 直接开始

双击同目录的 **习惯花园-双击即玩.html**，用 Chrome、Edge 或 Safari 打开即可。图片、样式和游戏代码已经全部打包在一个文件里，不需要安装依赖、启动 Cocos 或连接网络。

开发源文件入口为 `index.html`，请保留旁边的 `assets`、`model.js`、`app.js` 和 `style.css`。

## 玩法

1. 点击空土地，在土地上方的气泡中查看植物库存。单击植物播种当前土地，向外拖出植物可划过多块空地连续播种，操作后气泡消失。横向滑动气泡可查看其余植物；气泡固定宽度，展示约 3.5 个植物。第一株免费，3 秒成熟。
2. 点击成熟植物进入收获模式，再拖动收获工具划过成熟植物；也可逐块点击收获。
3. 点击右上角仓库，在收获物页选择数量出售。全部出售会再次确认。
4. 通过收获积累经验。升到 2 级后，可购买卡皮巴拉、开垦第 5 块土地。继续经营至 5 级解锁所有内容。
5. 商店购买的装饰进入仓库。在装饰仓库点击“放入花园”，点击亮起的草地放置，点击“确定”保存。
6. 编辑花园时点击已摆放的装饰或动物，可拖动、点击新位置移动，或收回仓库。“取消”恢复编辑前的全部布置和动物状态。

浏览模式下拖动地图探索，鼠标滚轮、右侧加减按钮、触屏双指可缩放。拖动播种、收获和编辑期间地图锁定。播种松手后自动返回浏览，不需要完成按钮；收获模式仍可点击“完成”返回。

## 完整内容

- 1—5 级、5 种植物、四阶段成长、8 块土地。
- 卡皮巴拉：出场期间新播种植物成长时间减少 20%，向上取整。收回不会改变已有植物的时间。
- 熊猫：出场期间收获物出售单价提高 10%，四舍五入为整数。两只动物可同时出场。
- 5 种装饰，每种最多拥有 5 件。网格摆放、碰撞限制、收回、确认和取消。
- 引导、经验升级、新解锁提示、收获飞入反馈、轻柔操作音效、应急种子。
- 自动本地存档、离线成长、导出和导入存档、二次确认重置。

所有经济数值和等级门槛按照用户提供的 PRD V1.0；不绑定学习任务、课程或阳光奖励。

## 存档说明

进度保存在**当前浏览器**，无需账号。关闭页面期间植物继续成长，成熟后不会枯萎。

不同浏览器、文件入口和本机服务入口可能使用不同的存储空间。建议固定使用同一个入口；换设备、换浏览器或移动文件前，先在设置中“导出存档”，再到新入口“导入存档”。浏览器禁止本地存储时会显示提示。

## 源码与验证

- `model.js`：配置、成长、库存、经验、交易、土地、动物和放置规则。
- `app.js`：页面、Pointer Events 拖动交互、地图、编辑事务、存档和音效。
- `style.css`：手机竖屏和桌面布局。
- `assets/coral/`：已确认珊瑚小屋画风的 48 张网页素材；动物为小鸟卡皮巴拉和闭眼熊猫。
- `tests/model.test.cjs`：规则验证，可运行 `node --test tests/model.test.cjs`。
- `tests/browser.cjs`、`tests/advanced.cjs`、`tests/progression.cjs`：浏览器流程验证脚本，需本机 Playwright 和 Chrome。
- `build-standalone.py`：运行 `python3 build-standalone.py` 重新生成双击即玩的 HTML。

如需本机网页服务，可在当前目录运行：

```sh
python3 -m http.server 4319 --bind 127.0.0.1
```

然后打开 http://127.0.0.1:4319 。该地址仅供本机访问，不是公网部署地址。

## 屏幕尺寸

默认桌面预览为 375×812 CSS 像素（窗口不足时缩短高度）。手机竖屏随可用屏幕铺满；iPad 使用全高、居中的游戏区域，横屏时保留两侧留白，避免横向拉伸花园。平板图标与弹窗按触控尺寸调整。
